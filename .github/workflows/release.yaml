name: Release
on:
  push:
    tags:
      - "*-*-*-*"
jobs:
  build:
    runs-on: windows-2022
    steps:
    - name: "Setup msvc"
      uses: ilammy/msvc-dev-cmd@v1
    - name: "Clone source"
      uses: actions/checkout@v4
      with:
        submodules: 'false'
    - name: "Build"
      run: |
        Rename-Item -LiteralPath "C:\\Strawberry" -NewName "C:\\fuckyou"
        mkdir build
        cd build
        cmake -G "NMake Makefiles" "-DCMAKE_BUILD_TYPE:STRING=Release" ..
        cmake --build .
    - name: "Package"
      shell: bash
      run: |
        mkdir -p bin/
        cp build/ritobin_cli/ritobin*.exe bin/
        cp build/ritobin_gui/ritobin*.exe bin/
        mkdir -p bin/hashes
        python scripts/update_hashes.py bin/hashes
        cp scripts/update_hashes.py bin/
        cp scripts/update_hashes_gui.py bin/
        sha256sum bin/*.exe
        7z a ritobin.zip bin/
    - name: "Release"
      uses: softprops/action-gh-release@v2
      with:
        files: ritobin.zip
